Template.menu.events({
    'click .closer': function (e) {
        // $('.menu').fadeOut(200);
        $('.menutoggle').show();
        $('.menuclose').hide();
        // $('#hero').css('transform','translateX(-200px)');
    },

});
